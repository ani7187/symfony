{% extends 'base.html.twig' %}

{% block body %}

    <div class="container">
        <div class="row">
            <div class="col-md-9">
                <h3 class="mt-5 mb-3 float-right">Add to do 🖍</h3>
            </div>
            <div class="col-md-6 offset-md-3">
                {{ form_start(form) }}
                    <div class="form-group">
                        {{ form_label(form.title) }}<br>
                        {{ form_widget(form.title,{'attr':{'class':'form-control','placeholder':'Title for your ToDo'}}) }}
                    </div>
                    <div class="form-group">
                        {{ form_label(form.description) }}<br>
                        {{ form_widget(form.description,{'attr':{'class':'form-control','placeholder':'Description for your ToDo'}}) }}
                    </div>
                    <div class="form-group float-right">
                        <button class="btn btn-danger mt-2 text-while">Add to list</button>
                    </div>
                {{ form_end(form) }}
            </div>
        </div>
    </div>

{% endblock %}