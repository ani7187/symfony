{% extends 'base.html.twig' %}

{% block body %}

    <div class="container">
        <div class="row">
            <div class="col-md-6 offset-md-3">
                <h3 class="mt-5 mb-3">ToDo 📝</h3>
            </div>
        </div>
        <div class="row">
            <div class="col-md-6 offset-md-3">
                {{ form_start(form) }}
                    <div class="form-group">
                        {{ form_label(form.title) }}<br>
                        {{ form_widget(form.title,{'attr':{'class':'form-control','placeholder':'Title for your ToDo','disabled':true}}) }}
                    </div>
                    <div class="form-group">
                        {{ form_label(form.description) }}<br>
                        {{ form_widget(form.description,{'attr':{'class':'form-control','placeholder':'Description for your ToDo','disabled':true}}) }}
                    </div>
                    <div id="js-edit-view">
                        <h3 class="mt-2 mb-3 float-right">Edit 🖍</h3>
                    </div>
                    <div class="form-group float-right" id="js-edit-btn" style="display: none">
                        <button class="btn btn-danger mt-2 text-while">Edit ToDo</button>
                    </div>
                {{ form_end(form) }}
            </div>
        </div>
    </div>

{% endblock %}